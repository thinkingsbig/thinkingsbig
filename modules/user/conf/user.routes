# Routing definition for flight module
GET        /subfolder/page        controllers.user.Application.getPage()

## Map static resources from the /public folder to the /assets URL path
GET    /*file    controllers.user.Assets.versioned(path="/public", file: Asset)
